# GOAI 研究终端设计记录

## 设计方向

GOAI 被定义为固定桌面工作站，而不是普通 Web UI、AI 控制台或交易营销页。参考机构终端的密度、指令栏和网格纪律，但不复制品牌、标识或具体界面。阅读顺序是：先扫顶部报价带，再看中央损益主图、平值报价、策略敏感度与期权链，右侧持续核对结论、风险与证据。

## 视觉系统

- 壳层：Monokai Dimmed 的深灰石墨三层底色、顶部主模块导航、左侧研究工作区树、中部高密度市场工作台、右侧结论账本；顶部和底部各有一条固定命令/状态带。
- 文字：系统无衬线负责中文标签，价格、时间、代码、状态和表格数字使用等宽字体；密度优先于装饰。
- 信号色：底色使用 Monokai Dimmed `#1E1E1E / #252526 / #2A2B2B`；亮绿 `#A6E22E` 用于当前选择与行动，青色 `#66D9EF` 承载图表路径，暗绿 `#879A3B` 表达通过，红 `#BE3F48` 表达亏损或阻断，黄 `#C5A635` 只保留给警告。
- 形态：矩形分区、1px 线、紧凑表格和固定列关系；不使用渐变、玻璃、同尺寸卡片墙或大胶囊。
- 壳层职责：左侧管理任务和资料入口，中间是报价→主图与平值报价→Greeks→期权链→风险/事件账本，右侧持续保留结论、检查项、事件和来源。
- 终端语言：支持 `0700 HK <GO>`、`F5`、`REFRESH` 和自然语言场景；命令解释与数据加载状态必须在底部状态栏可见。
- 动效：只保留命令反馈、视图切换和运行阶段，并遵守减少动效设置。

## 反模板审计

- 首屏不展示供应商名、内部门状态、token、虚构快捷键或营销式 AI 话术；这些信息只在需要审计的运行记录中出现。
- 模块副标题、检查栏和数据源改用中文工作语言；`Edge`、`Risk`、`NO_TRADE`、`LOW_EDGE` 等内部枚举不进入用户主视觉。
- 右侧检查栏使用连续分隔线而不是嵌套卡片，运行记录使用文本行而不是聊天气泡，来源状态使用行内文字而不是胶囊标签。

## 文案原则

引擎内部枚举保留在数据状态和 DOM 属性中，首屏只显示自然语言：

| 内部状态 | 用户看到的文字 |
|---|---|
| `NO_TRADE` | 这次先不交易 |
| `LOW_EDGE` | 机会不足 |
| `PASS` | 风险可控 |
| `BLOCK` | 被风险拦截 |
| `DRAFT_ONLY` | 暂存为草案 |
| `READY_FOR_CONFIRMATION` | 可以进入确认 |

三段判断改为“机会是否值得、最坏情况是否可承受、下一步怎么做”。原始状态通过 `data-system-code` 保留，便于测试和审计，但不作为用户主视觉。

## 视觉资产

- 文件：`ui/assets/goai-signal-field.png`
- 提示词：`ui/assets/goai-signal-field.prompt.md`
- 用途：右侧研究上下文中的事件前波动场抽象图。它不代表行情、不承载数值，也不替代图表。
- Image 2 母稿：`ui/assets/goai-ui-redesign-reference.png`
- 初版 Bloomberg-inspired 终端参考图：`ui/assets/goai-terminal-reference.png`。
- 已实施的高级终端母稿：`ui/assets/goai-terminal-premium-reference.png`；完整提示词保存在同名 `.prompt.md` 文件中。

## 交互和契约

保留原有 `/api/state`、`/api/policy-library`、`/api/run`、`/api/chat` 和静态快照回退，并新增 `/api/command` 作为桌面终端命令入口。前端继续只做格式化和渲染，所有金融数字仍由服务或确定性引擎提供。当前快照未录制历史行情时，图表显示覆盖边界，不绘制虚构 K 线。实时行情、模拟动作和订单提交不被视觉重构隐式开启。

## 验收记录

- `node --check ui/app.js` 通过。
- `python -m pytest tests/test_ui_server.py -q` 通过，15 项通过。
- 桌面和 390px 移动视口已复核。
- 总览、期权链视图切换，以及场景解析抽屉打开和关闭已复核。
- Impeccable 检查发现的侧边高亮已改为横向分隔；未保留检测到的 AI 卡片式边缘装饰。
